import { KlTournamentEnhanced, ParticipantInfo } from '../types/db';
import { GenericTournament } from "./GenericTournament";
import { ScheduledMatchWindow } from "../types/tournament";

export class TournamentSingleElimination extends GenericTournament {
  // public CreateNextPhase(fullTournamentInfo: KlTournamentEnhanced, participants: ParticipantInfo[]) {
  //
  // }
}

